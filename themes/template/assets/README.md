# assets

Local images and fonts your theme references from CSS, for example
`background: url(assets/paper.png)`. They are embedded as data URIs when a document is
converted, so keep them small; remote URLs are refused because a generated document
must stay self-contained.
